<muxc:TabViewItem
    x:Class="Celbridge.Legacy.Views.TextFileDocumentView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Celbridge.Legacy.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    DataContext="{x:Bind ViewModel}">

    <muxc:TabViewItem.Header>
        <TextBlock Text="{x:Bind ViewModel.Name, Mode=OneWay}" />
    </muxc:TabViewItem.Header>

    <Grid Background="{ThemeResource PanelBackgroundABrush}"
          VerticalAlignment="Stretch">

        <WebView2 Grid.Row="1" 
                  x:Name="EditorWebView" 
                  Visibility="Collapsed"
                  DefaultBackgroundColor="{ThemeResource PanelBackgroundAColor}"/>
    </Grid>
</muxc:TabViewItem>
