<UserControl
    x:Class="Celbridge.Legacy.Views.MainMenu"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Celbridge.Legacy.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:win="http://schemas.microsoft.com/winfx/2006/xaml/presentation">

    <Grid>
        <Button ToolTipService.ToolTip="Main Menu"
                ToolTipService.Placement="Right">
            <SymbolIcon Symbol="GlobalNavigationButton"/>
            <Button.Flyout>
                <MenuFlyout Placement="RightEdgeAlignedTop">
                    <MenuFlyoutItem Text="New Project" Icon="Add" Command="{x:Bind ViewModel.NewProjectCommand}"/>
                    <MenuFlyoutItem Text="Open Project" Icon="OpenFile" Command="{x:Bind ViewModel.OpenProjectCommand}"/>
                    <MenuFlyoutItem Text="Close Project" Icon="ClearSelection" Command="{x:Bind ViewModel.CloseProjectCommand}"/>
                    <MenuFlyoutSeparator/>
                    <MenuFlyoutItem Text="Settings" Icon="Setting" Command="{x:Bind ViewModel.OpenSettingsCommand}"/>
                    <win:MenuFlyoutItem Text="Exit" Icon="Cancel" Command="{x:Bind ViewModel.ExitCommand}"/>
                </MenuFlyout>
            </Button.Flyout>
        </Button>
    </Grid>
</UserControl>
