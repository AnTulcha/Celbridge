<UserControl
    x:Class="Celbridge.Inspector.Views.VoiceLineView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Celbridge.Inspector.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006">

    <!-- Detail panel -->
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="30"/>
        <RowDefinition Height="auto"/>
      </Grid.RowDefinitions>

      <Grid Grid.Row="0"
            Background="{ThemeResource SystemAccentColor}"
            BorderBrush="{ThemeResource PanelBorderBrush}"
            BorderThickness="0,0,0,2">
        <TextBlock Text="Voice Line"
                   FontWeight="Bold"
                   Margin="4"
                   VerticalAlignment="Center"/>
      </Grid>

      <StackPanel Grid.Row="1"
                  Orientation="Vertical"
                  Spacing="4">
        <TextBox Header="Speaker"
                 Text="Darth Vader"/>
        <TextBox Header="SpeakingTo"
                 Text="Luke Skywalker"/>
        <TextBox Header="Line"
                 Text="No, I am your father!"/>
        <TextBox Header="Direction"
                 Text="Shake fist meaningfully"/>
      </StackPanel>

</Grid>
  
</UserControl>
