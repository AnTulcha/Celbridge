<UserControl
    x:Class="Celbridge.Views.InstructionLinePropertyView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Celbridge.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto" MinWidth="80"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0"
              Width="{x:Bind ViewModel.IndentWidth, Mode=OneWay}"/>
        <TextBox Grid.Column="1" 
                 x:Name="KeywordTextBox"
                 Text="{x:Bind ViewModel.Keyword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 Foreground="{x:Bind ViewModel.KeywordColor, Mode=OneWay}"
                 Margin="0"
                 GotFocus="KeywordTextBox_GotFocus"/>
        <TextBlock Grid.Column="2" 
                   x:Name="DescriptionTextBlock"
                   VerticalAlignment="Center"
                   Margin="6 0 0 0"
                   ToolTipService.Placement="Bottom"
                   ToolTipService.ToolTip="{x:Bind ViewModel.Tooltip, Mode=OneWay}"/>
    </Grid>

</UserControl>
