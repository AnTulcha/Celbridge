<UserControl
    x:Class="Celbridge.Views.ExpressionPropertyView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Celbridge.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:utils="using:Celbridge.Utils"
    mc:Ignorable="d"
    xmlns:win="http://schemas.microsoft.com/winfx/2006/xaml/presentation">

    <UserControl.Resources>
        <utils:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" 
                   Text="{x:Bind ViewModel.LabelText}"
                   VerticalAlignment="Center"
                   MinWidth="80"
                   Visibility="{x:Bind ViewModel.HasLabelText, Mode=OneWay,Converter={StaticResource BoolToVisibilityConverter}}"/>
        <TextBox Grid.Column="1" 
                 x:Name="ValueTextBox"
                 Text="{x:Bind ViewModel.ExpressionValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 IsSpellCheckEnabled="False"
                 Margin="6 0 0 0"/>
    </Grid>

</UserControl>
